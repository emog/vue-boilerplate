export const state = {
    count: 0,
};

// getters
export const getters = {};

// mutations
export const mutations = {
    increment (state) {
        state.count++;
    }
};

// actions
export const actions = {};
